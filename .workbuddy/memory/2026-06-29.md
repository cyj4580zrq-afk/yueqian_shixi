## 2026-06-29

### 修复 ui_client.c 触摸阻塞（死锁）

**根本原因**： 和  在持有  的同时调用了阻塞 I/O（ + ）， 在用户点击时尝试获取同一把锁，造成死锁。

**修复方法**：将所有阻塞 I/O（SSH popen/fgets、wpa_cli popen/fgets 循环）移到锁外，使用线程局部变量收集结果，最后在极短的临界区内写入共享变量并重绘 UI。

**修改文件**：
- ：popen(ssh) + fgets 移到锁外，用 local_wuhan/local_beijing 暂存，最后加锁写入
- ：popen(wpa_cli) + fgets 循环移到锁外，用 local_ssid[]/local_signal[] 暂存，最后加锁批量写入

**编译**： 编译成功（0 error 0 warning）
**部署**：板子 SSH 不通（未开机），产物在 ，待板子起来后执行 

